import { useEffect } from "react"

export default function MatchesList(){
  useEffect(()=>{
     // api запрос через CreateApi redux'a const matches = useGetMatchesQuery()

  }, [])

  return (
   <>
     <ul>

     </ul>
   </>
  )
}